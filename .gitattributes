# Normalize text files in the repo; Git will convert to CRLF on Windows working copies if configured
*               text=auto

# Enforce LF in repo for common text
*.md            text eol=lf
*.yml           text eol=lf
*.yaml          text eol=lf
*.py            text eol=lf
*.sh            text eol=lf
*.json          text eol=lf
*.svg           text eol=lf
*.css           text eol=lf
*.js            text eol=lf
*.ts            text eol=lf

# Windows scripts keep CRLF for convenience
*.bat           text eol=crlf
*.cmd           text eol=crlf
*.ps1           text eol=crlf

# Treat binaries as binary; disable diffs
*.png           binary -diff
*.jpg           binary -diff
*.jpeg          binary -diff
*.gif           binary -diff
*.pdf           binary -diff
*.docx          binary -diff
*.pptx          binary -diff
*.xlsx          binary -diff
*.zip           binary -diff
*.7z            binary -diff

# Mark documentation folders so GitHub Linguist doesn’t skew language stats
docs/**                     linguist-documentation
content/volumes/**          linguist-documentation
